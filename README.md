# transition-zone
Provides functionality to determine transition boundary between surge, river, or compound dominated coastal dynamics.

## Installation 
Everything is prepped via the conda yaml file. It may take a while to install all the dependencies. This yaml file was used for a unix build and may not be portable. Stay tuned for updates. 
